{
    "name": "email-service",
    "version": "1.0.0",
    "description": "Payment Gateway MVP - Email Service",
    "main": "dist/main.js",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "start": "node dist/main.js",
        "start:dev": "ts-node-dev --respawn --transpile-only src/main.ts"
    },
    "dependencies": {
        "amqplib": "^0.10.0",
        "nodemailer": "^6.9.0"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "@types/amqplib": "^0.10.0",
        "@types/nodemailer": "^6.4.0",
        "typescript": "^5.0.0",
        "ts-node-dev": "^2.0.0"
    }
}